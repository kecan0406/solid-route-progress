---
'solid-route-progress': patch
---

Accept `@solidjs/router` 0.15 in the peer range. The router integration uses the same `useIsRouting()` and `useBeforeLeave()` behavior there, and the router tests now also run against 0.15.
