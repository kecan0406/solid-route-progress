---
'solid-route-progress': patch
---

Cap the `@solidjs/router` peer range at 1.x (`^0.15.0 || ^1.0.0`), matching `solid-js`: Solid 2 is not supported yet, and the docs now say so. The package also carries the `tanstack-intent` keyword, so TanStack Intent finds the agent skill it ships.
