export default function About() {
  return (
    <>
      <h1 class="mb-3 text-3xl font-semibold tracking-tight">About</h1>
      <p class="text-zinc-600">
        This page loads instantly, so no bar was drawn: navigations shorter than <code>delay</code>{' '}
        (200 ms by default) never show it.
      </p>
    </>
  )
}
